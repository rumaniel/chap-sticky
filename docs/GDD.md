# 찹! 찐득이 — 콘텐츠 GDD (모형·맵)

> 코드 레지스트리(`shapes.js`/`materials.js`)는 **무엇**이 있는지는 선언적으로 보여주지만,
> **왜 이 수치인지·어떤 플레이 정체성을 노렸는지·밸런스 실측치**는 담지 못한다. 이 문서가 그 간극을 메운다.
> 수치를 바꾸면 이 문서의 "정체성"과 어긋나는지 먼저 확인할 것.

## 0. 코어 공식 (튜닝의 기준점)

```
부착 판정   : 충격량 (vz + 0.35×접선속도)×질량  vs  한계 Σ(접촉패드 grip × 그 위치 머테리얼 grip) × K_MAX(2.45)
품질 q      : 충격량/한계 비율 r의 종형 곡선. SWEET=0.62에서 최대. 과속 쪽 완만(0.55)
기하 계수   : 패드별 0.72~1.28 (결정론) — 가로 미끄러짐 진행 쪽 강 / 반대쪽 약 / 커브 스핀 쪽 약.
              수직 성분은 제외(전 던지기 공통이라 편향만 생김). 조준·커브 미세 차 → 매판 다른 붕괴 순서
패드 HP     : min(1.2, (0.55+0.7×품질그립)×머테리얼grip) × 기하계수 × 지터   ※캡은 베이스에만 (분산 보존)
지터 폭     : ±(15% + 18%×(1−q)) — 퍼펙트 안정, 슬로피 카오스 (주스도 ±(5%+10%×(1−q)))
주스(수명)  : (0.45 + 1.0×품질그립) × shape.juiceMod → 재부착마다 −(0.11 + 0.02×롤속도)
재부착 HP   : 주스 × 1.45 × 머테리얼 grip × (0.78~1.28 랜덤)
미끄덩      : hold 중 확률 0.35×(1−q)×경과가중 — 저품질 부착의 이산 슬립 이벤트 (유리 제외)
롤 방향     : 매 롤 P(우) = 0.5 + 커브바이어스(driftX×30, 롤마다 ×0.55 감쇠) + 좌우HP비대칭×0.25
              + 직전방향×0.12, 클램프 [0.15,0.85] — 커브는 초반 지배, 이후 지그재그 (영구 고정 없음)
점수        : Σ[초당 15 × 현재 링 배율] + 스팟 보너스 + 커브(60+스핀×6) + 퍼펙트(200)
퍼펙트 판정 : |충격비 r − SWEET| < 밴드(모형×맵). 밴드 = PERFECT_BASE(0.0245) × 민감도 배율
              민감도 배율 = sweet ±4% 구간에서 r(s) 의 할선 기울기 / KZ 기울기 (clamp 0.35~1.80)
              고정 폭이면 안 되는 이유: 접선 항이 s0 = √(g·d/KZ/KY) ≈ 1.29 에서 부호를
              뒤집는다(도착이 궤적 정점). 그 아래면 세게 던질수록 |vy| 가 줄어 r 이 둔감해져
              세기 기준 퍼펙트 구간이 3배 넓어진다. 고정 폭이던 시절 냉장고만 62% 였다.
              던지기마다 재면 정점 부호 컷 때문에 구간이 섬 4개로 쪼개진다 — 조합당 1값.
              실측 9~25%, 세기 기준 폭 7.9~8.9% (R10 2026-09-01, R15 2026-09-02)
게이지 표시  : limit = 한계/질량/TANGENT_DIV(1.21)/KZ — 최악(비스듬한 던지기) 기준 안전 하한
              sweet = r(s)=SWEET 의 닫힌 해 (이차식). 상수로 나누면 맵마다 ±0.03 오차가 나
              마커대로 던져도 퍼펙트가 안 나온다 (R14·R15). 근이 없는 저그립이면 최소점.
              수식은 전부 sticky.js (ST.Sticky.gaugeBand / perfectBand) — 게임과 시뮬 공용.
벽 충돌      : 서브스텝 크기 고정(≤1/120s, 60fps 는 2스텝) + 서브스텝 안에서 z=WALL_Z 로
              위치·속도·angle·spin 선형 보간 (physics.js stepFlight). 프레임 스케줄과 무관하게
              같은 던지기는 비트 단위로 같은 착탄 — 히치 1,728케이스 Δpos = 0 (R15)
              검증: node tools/sim/gauge.js (검사 5종)
```

- **스윗스팟을 넓히려면**: `K_MAX`↑(튕김 감소) 또는 `SWEET_HARD`↑(과속 관용)
- **보너스 스팟 배치 제약** 두 가지
  1. 크롤 경로가 지나는 구간 안에 둔다. 도달 가능 영역은 벽 정규화 좌표로
     **x 0.23~0.77 / y 0.22~0.95**. 밖에 두면 영영 닿지 않는다 (R9, 2026-09-01)
  2. **판정 원끼리 겹치면 안 된다.** 판정 반경은 그려지는 원보다 크다
     (`r × min(WALL_W,WALL_H) + TOY_R × 0.5`). 겹치면 두 스팟 어디에도 안 닿아 보이는
     자리에서 둘 다 터진다. `r=0.06` 이면 중심거리 **> 0.50m** (R13, 2026-09-02)
- **라이드 길이(바닥 도달)를 늘리려면**: `JUICE_Q`↑ 또는 `JUICE_COST`↓
- **크롤 템포**: `TOPPLE_RATIO`↑ = 빨리 넘어감, 머테리얼 `decay`↑ = 스탠스 짧아짐

## 1. 모형 (Shapes)

공통: HP는 모형 간 정규화(패드 grip은 부착 판정에만 기여). 개성은 `decayMod`·`rollStep`·패드 기하가 만든다.

### 1.1 찐득맨 `man` — 기본기, 온보딩 기준점
| 항목 | 값 | 의도 |
|---|---|---|
| 질량 | 1.0 | 기준 |
| decayMod | 1.0 | 기준 |
| rollStep | 180° | 시원한 끝-넘기 한 방 — "찐득이" 하면 떠오르는 그 동작 |
| 패드 | 손×2(±0.92,−0.50) grip1.0 · 발×2(±0.50,0.92) grip1.0 | 손↔발 2그룹 교대 플립. 위(손)부터 벗겨져 topple 리듬이 읽기 쉬움 |
| 해금 | 0 | — |

정체성: **균형·가독성**. 모든 튜닝의 기준. 회당 하강 ~0.31m.

### 1.2 문어찐득 `octo` — 끈질긴 탱커
| 항목 | 값 | 의도 |
|---|---|---|
| 질량 | 1.1 | 살짝 무거워 부착 판정 미세 불리 (고그립 맵 선호 유도) |
| decayMod | 0.85 | 오래 버팀 |
| rollStep | 90° | 짧은 플롭 여러 번 — 다리 많은 생물감 |
| 패드 | 전방위 방사형 8지점(45° 간격, r 0.78/0.92 교대) grip0.55 | 어느 방향으로 굴러도 다음 다리가 닿음. 부분 노그립에 강함 |
| 해금 | 5,000 | 첫 해금 목표 (~7-8라운드) |

정체성: **지구전**. 칠판×문어 = 최장 홀드 조합 (실측 ~13-15s).
주의: 방사형 배치가 물리 전제 — 하반원 밀집으로 되돌리면 롤이 강제종료로 퇴화한다 (커밋 f38de91 참조).

### 1.3 별찐득 `star` — 가볍고 화끈한 데굴이
| 항목 | 값 | 의도 |
|---|---|---|
| 질량 | 0.8 | 가벼워 잘 붙음 (충격량↓) — 부착 관용 |
| decayMod | 1.2 | 빨리 닳음 — 붙긴 쉬운데 오래는 못 버팀 |
| juiceMod | 1.12 | 롤 여력 보정 — "데굴데굴 라이드" 정체성 유지 (품질 지터 축소로 잘린 롱라이드 꼬리 보상) |
| rollStep | 72° | 오각형 데굴데굴 — 회전 자체가 구경거리 |
| 패드 | 꼭짓점 5지점 grip0.75 | 대칭 롤링 기하 |
| 해금 | 15,000 | 후반 보상 |

정체성: **하이 템포 고득점**. 롤 횟수 최다(평균 5회) → 링 통과 점수 이득. 실측: 같은 조건 최다 점수 경향.

### 1.4 신규 모형 추가 절차
1. `shapes.js` SHAPES에 항목 추가: `{id, unlock, mass, decayMod, rollStep, stickyPoints:[{id,x,y,grip}], color..., draw(ctx,S,opt)}`
2. 패드는 **로컬 단위좌표(-1~1, y아래+)**. 대칭 배치 권장 — 롤 완료 기하가 자연스러움
3. `i18n.js`에 `shape.<id>.name/desc` (ko/en)
4. `ST.Shapes.list` 순서에 삽입 (카드 노출 순서)
5. 시뮬 스니펫(ARCHITECTURE.md §7)으로 stick율·홀드·바닥도달률 확인

## 2. 맵 (Maps/Materials)

머테리얼 5속성: `grip`(접착 배율) `decay`(그립 감쇠/s) `flipPeriod`(레거시—현재 v2에선 미사용) `slideStep`(플립 하강 배율) `slideCont`(연속 미끄럼) `bounce`(튕김 반발).
멀티 머테리얼: `zone`(유효 영역) + `noGrip[]`(내부 노그립 사각형들) → `materialAt(x,y)`가 위치별 머테리얼/null 반환.

### 2.1 교실 칠판 `chalk` — 첫 맵(튜토리얼), 붙박이 명당
| grip | decay | slideStep | 존 | 과녁 | 해금 |
|---|---|---|---|---|---|
| 1.3 | 0.042 | 0.85 | 나무테두리 노그립(zone 4.5% 인셋) | 중앙, ×4~×1 | **0 (기본 선택)** |

의도: **관대한 첫 경험** — 세게 던져도 붙는다(6/7 스펙트럼). 오래 붙어 점수 쌓는 "명당" 정체성 → 바닥 라이드는 드묾(0%)이 맞다. 스팟: 100점·A+ (+120) — 교실 테마.

### 2.2 거실 벽 `room` — 표준, 라이드 맵
| grip | decay | slideStep | 존 | 과녁 | 해금 |
|---|---|---|---|---|---|
| 1.0 | 0.055 | 1.0 | 없음(전면) | 중앙, ×5~×1 | 0 |

의도: **기준 맵 + 데굴데굴 라이드** — 잘 던지면 50%가 바닥까지 완주(실측). 온보딩 후 "진짜 게임" 시작점.

### 2.3 유리창 `glass` — 스킬 체크, 주르륵
| grip | decay | slideCont | 노그립 | 과녁 | 해금 |
|---|---|---|---|---|---|
| 0.90 | 0.085 | 26 | 창틀 십자(폭 3.2%) | **우상 판유리**, ×6~×1 | 3,000 |

> grip 은 0.85 였는데 2026-09-02 에 0.90 으로 올렸다(R15). `r=SWEET` 의 닫힌 해는
> grip 0.831 에서 판별식이 0 이 되는데 0.85 는 그 절벽 바로 위라, sweet 이 하강
> 구간 깊숙이(0.97) 내려앉아 낮게 착탄 → 창틀에 걸렸다(부착률 41%). 0.90 이면
> sweet 1.178 로 예전 값 근처다. 그립은 냉장고와 같은 최저 타이지만 창틀·미끄럼 때문에
> 부착률(70.3%)·버티기(4.2s)는 압도적 최저라 "스킬 관문" 정체성은 유지된다.
> **이 값을 다시 내릴 거면 절벽 여유(현재 8%, `node tools/sim/gauge.js` 검사 5)를 먼저 볼 것.**

의도: **조준 스킬 관문** — 십자 창틀을 피해 판유리 쿼드런트에 살살. 중앙 정통은 양옆 패드만 걸려 잠깐 붙다 주르륵(창발). 과녁을 창틀 위에 두지 말 것(불스아이 도달 불가 이슈로 이동한 이력). 미끄럼 특성상 바닥 도달 최다(100%) — "주르륵"이 정체성.

### 2.4 냉장고 `fridge` — 잭팟 스팟
| grip | decay | slideCont | 노그립 | 과녁 | 해금 |
|---|---|---|---|---|---|
| 0.9 | 0.05 | 4 | 손잡이(우측 9%) | 좌상 편심, ×5~×1 | 12,000 |

의도: **스팟 헌팅** — 자석 3개(+300)가 링보다 크게 벌어준다. 링과 자석이 떨어져 있어 "안정 링이냐 잭팟 자석이냐" 선택. 8개 자석 장식이 ×3 링을 은유(ringStyle.magnets).

### 2.5 신규 맵 추가 절차
1. `materials.js` MAPS에 항목: `{id, unlock, mat:{...}, zone?, noGrip?, rings, ringStyle, spots, skyColor, floorColor, drawWall(ctx,W,H)}`
2. `rings.cy`는 **자연 착탄 고도(월드 1.3~1.8m)** 근처에: cy 0.46~0.52 권장. 노그립 위에 불스아이 금지
3. `i18n.js`에 `map.<id>.name/desc`, `ST.Materials.list` 순서 반영
4. 밸런스 가늠: 한계 = 4패드×grip×2.45 → 최속 플릭 충격량(~11)과 비교해 튕김 구간 설계
5. 시뮬로 stick율(목표: 표준맵 5~6/7 스펙트럼)·홀드·바닥률 확인

## 3. 밸런스 매트릭스 (실측, 2026-08-08 크롤 다이나믹스 반영)

good=−1.4 / sloppy=−1.85. 바닥 = 부착 상태 최저 y < 0.38m (벽 하단 필오프 지점):

| 조합 | 부착률 | 평균 홀드 | 평균 롤 | 바닥 도달률 |
|---|---|---|---|---|
| 맨×칠판 good | 18/18 | 10.2~10.6s | 3.0 | 0% (붙박이) |
| 맨×거실 good | 50/50 | 8.3~8.8s | 3.6 | **55~75% (라이드, 지그재그로 소폭↑)** |
| 맨×거실 sloppy | 24/24 | 6.6~7.7s | 2.1~3.0 | 0% + **조기 붕괴(3~4s) ~15%** |
| 문어×칠판 good | 6/6 | 13.0s | 4.0 | 0% (최장 홀드) |
| 별×거실 good | 28/28 | 8.3~9.9s | 3.9~5.0 | ~21% (런 간 편차 큼 — 꼬리 사건) |
| 맨×유리 쿼드런트 | 8/8 | 5.1s | 3.0 | 주르륵 하강 후 하부 낙하 |

설계 명제: **"잘 던지면 라이드, 대충 던지면 조기 탈락"** + **"같은 패턴이어도 매판 다른 전개"** —
good/sloppy 바닥률 격차와 sloppy 홀드의 바이모달 분포(조기 붕괴 꼬리)가 핵심 지표.
품질별 패드 HP 분산 실측: 퍼펙트 CV 8~9% / 미드 ~13% / 슬로피 ~18% (베이스 캡 뒤 지터라 분산 보존).

## 4. 튜닝 상수 사전 (sticky.js V2)

| 상수 | 값 | 올리면 |
|---|---|---|
| K_MAX | 2.45 | 튕김 줄고 부착 관대 |
| SWEET / SWEET_HARD | 0.62 / 0.55 | 최적 충격 위치 / 과속 관용 |
| PADK | 6.0 | 전체 그립 소모 빨라짐 |
| LOAD_TOP | 0.85 | 위 패드 먼저 죽는 경향 강화 |
| TORQUE_STRESS | 1.35 | 아래-지지 자세 수명 감소 |
| TOPPLE_RATIO | 0.35 | 더 일찍 넘어감(템포↑) |
| JUICE0 / JUICE_Q | 0.45 / 1.0 | 기본 수명 / 품질 보상 |
| JUICE_COST | 0.11 | 롤 상한 감소 |
| RESTICK_HP | 1.45 | 재부착 후 유지력 |
| ROLL_MAXPHI | 1.5π | 롤 안전장치 (건드리지 말 것) |
| GEO_ALIGN | 0.22 | 가로 미끄러짐 방향 패드 강/약 비대칭 (수직 성분 금지 — 편향) |
| GEO_SPIN | 0.14 | 커브 스핀 도는 쪽 사이드 약화 |
| JIT_BASE / JIT_Q | 0.15 / 0.18 | 지터 폭 = BASE + Q×(1−품질). BASE↓ = 스킬 안정 보상↑, 롱라이드 꼬리↓ |
| SLIP_RATE | 0.35 | 저품질 미끄덩 확률/s (×(1−q)×경과). 유리 제외 |
| ROLL_MOMENTUM / ROLL_HP_BIAS | 0.12 / 0.25 | 방향 관성 / 약한 쪽 쏠림. 올리면 한 방향 유지 경향↑ |
| CURVE_BIAS / CURVE_ROLL_DECAY | 30 / 0.55 | 커브 방향 지배력 / 롤당 감쇠 (0.55 = 1~2롤 후 자유) |
| (shapes) juiceMod | 별 1.12 | 모형별 롤 여력 — 라이드 정체성 튜닝 손잡이 |

방향 전환율 실측: 거실 무커브 48% / 우커브 첫 롤 80% 스핀 방향 → 이후 40% 전환 / 칠판 19%(롤 적음) / 별 33%.

주의: 패드 HP 캡 min(1.2,·)은 **베이스에만** 적용할 것 — 지터·기하 계수 뒤에 걸면 좋은 던지기의
패드 분산이 통째로 뭉개져 "매판 똑같은 크롤"이 된다 (크롤 다이나믹스 이전의 경향성 원인 중 하나).
