name: Gemini review
description: Gemini CLI 로 PR 을 리뷰하고 인라인 코멘트를 단다.

inputs:
  gemini_api_key:
    description: Google AI Studio API key
    required: true
  github_token:
    description: PR 코멘트를 달 토큰
    required: true
  pr_number:
    description: 리뷰할 PR 번호
    required: true
  model:
    description: 모델 ID (비우면 CLI 기본값)
    required: false
    default: ''
  debug:
    description: Gemini CLI 디버그 로그
    required: false
    default: 'false'
  upload_artifacts:
    description: Gemini CLI stdout/stderr 를 워크플로 아티팩트로 올린다 (진단용)
    required: false
    default: 'false'

runs:
  using: composite
  steps:
    - uses: google-github-actions/run-gemini-cli@v0
      env:
        GEMINI_CLI_TRUST_WORKSPACE: 'true'
        GITHUB_TOKEN: ${{ inputs.github_token }}
        GEMINI_API_KEY: ${{ inputs.gemini_api_key }}
      with:
        gemini_api_key: ${{ inputs.gemini_api_key }}
        gemini_model: ${{ inputs.model }}
        gemini_debug: ${{ inputs.debug }}
        upload_artifacts: ${{ inputs.upload_artifacts }}
        workflow_name: gemini-review
        github_pr_number: ${{ inputs.pr_number }}
        # 리뷰 코멘트는 github-mcp-server 를 통해 단다. core 툴은 끄고
        # 필요한 3개만 열어 모델이 저장소를 건드리지 못하게 한다.
        settings: |-
          {
            "model": { "maxSessionTurns": 25 },
            "mcpServers": {
              "github": {
                "command": "docker",
                "args": [
                  "run", "-i", "--rm",
                  "-e", "GITHUB_PERSONAL_ACCESS_TOKEN",
                  "ghcr.io/github/github-mcp-server:v0.27.0"
                ],
                "includeTools": [
                  "add_comment_to_pending_review",
                  "pull_request_read",
                  "pull_request_review_write"
                ],
                "env": { "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}" }
              }
            },
            "tools": { "core": [] }
          }
        extensions: |
          [
            "https://github.com/gemini-cli-extensions/code-review"
          ]
        prompt: '/pr-code-review'

    # CLI 는 모델이 툴 호출에 실패해도 exit 0 으로 끝난다. stdout JSON 의
    # error 필드를 봐야 진짜 성공인지 알 수 있다. 여기서 실패로 바꿔야
    # 워크플로의 재시도가 걸린다.
    - name: Detect CLI error
      shell: bash
      run: |
        f=gemini-artifacts/stdout.log
        if [[ ! -f "$f" ]]; then
          echo "::error title=Gemini::stdout.log 이 없다 — CLI 가 실행되지 않았다"
          exit 1
        fi
        if jq -e 'has("error")' "$f" >/dev/null 2>&1; then
          echo "::error title=Gemini CLI error::$(jq -c '.error' "$f")"
          exit 1
        fi
        echo "리뷰 정상 종료"
